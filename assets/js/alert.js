const myName = 'Carlos';

// Mensaje previo en la parte superior
alert(myName);

// Ingresar un valor
const nombre = prompt('Ingresa tu nombre', 'John Doe');
console.log(nombre);

// En caso que el prompt sea cancelado regresa null
// Null y undefined no son iguales

// Permite confirmar una accion
const seleccion = confirm('Seguro que quiere salir');
console.log(seleccion);

